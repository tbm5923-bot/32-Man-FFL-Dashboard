# 32-Man FFL Dashboard — MIDA

Deployment repository for the McAllister Institute for Dynasty Analytics (MIDA) Streamlit dashboard.

The deployable application is being published on the `streamlit-deploy-v1` branch so the initial release can be verified before anything is merged into `main`.
